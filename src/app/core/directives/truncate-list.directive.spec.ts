import { TruncateListDirective } from './truncate-list.directive';

describe('TruncateListDirective', () => {
  it('should create an instance', () => {
    const directive = new TruncateListDirective();
    expect(directive).toBeTruthy();
  });
});
