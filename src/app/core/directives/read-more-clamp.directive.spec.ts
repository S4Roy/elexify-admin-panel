import { ReadMoreClampDirective } from './read-more-clamp.directive';

describe('ReadMoreClampDirective', () => {
  it('should create an instance', () => {
    const directive = new ReadMoreClampDirective();
    expect(directive).toBeTruthy();
  });
});
